apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wekan-graphql-noauth.fullname" . }}-test-connection"
  labels:
{{ include "wekan-graphql-noauth.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "wekan-graphql-noauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
