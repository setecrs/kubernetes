apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
stringData:
  LDAP_BASE_DN: {{ required .Values.LDAP_BASE_DN }}
  LDAP_ADMIN_PASSWORD: {{ required .Values.LDAP_ADMIN_PASSWORD }}
  SID: {{ required .Values.SID }}
